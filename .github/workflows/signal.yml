name: DataFlow Arbitrage Signal

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DataFlow Arbitrage core is alive"
